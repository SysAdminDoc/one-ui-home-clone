# Copy to ../keystore.properties (gitignored) and fill in for local release builds.
# CI builds pull these from repository secrets — see .github/workflows/release.yml.
storeFile=prototype-android/release.keystore
storePassword=CHANGE_ME
keyAlias=oneuihomeclone
keyPassword=CHANGE_ME
